# Structured Prompt Pack

There are 18 independently executable session prompts. Run them in order. Each prompt contains its own deliverables, validation gate, evidence requirement and Git checkpoint.

Do not merge the prompts into one giant execution request.
