async function latest(){ try { const base=process.env.NEXT_PUBLIC_BASE_URL||"http://localhost:3000"; const r=await fetch(`${base}/api/telemetry/latest`,{cache:"no-store"}); return (await r.json()).reading; } catch { return null; } }
function Card({label,value}:{label:string,value:string}){return <div className="rounded-xl border p-4"><div className="text-sm opacity-70">{label}</div><div className="mt-2 text-2xl font-semibold">{value}</div></div>}
export default async function Home(){const x=await latest(); return <><h1 className="text-3xl font-bold">Live Status</h1><p className="mt-2 opacity-70">No value is fabricated when the device/database is unavailable.</p><div className="mt-6 grid gap-4 md:grid-cols-3"><Card label="Temperature" value={x?.temperature_c==null?"No data":`${Number(x.temperature_c).toFixed(2)} °C`}/><Card label="pH" value={x?.ph==null?"No data":Number(x.ph).toFixed(2)}/><Card label="PT550 light" value={x?.light_voltage_v==null?"No data":`${Number(x.light_voltage_v).toFixed(3)} V`}/></div></>}
