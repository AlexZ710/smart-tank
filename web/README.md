# Smart Tank Web Starter
- Next.js App Router + Tailwind CSS.
- Route Handler ingestion.
- PostgreSQL through `DATABASE_URL`.
- `npm install`, then `npm run dev`.
- Apply `../database/schema.sql` before ingestion.
- Set `DEVICE_INGEST_TOKEN` locally.
The placeholder pages are intentionally incomplete; S24-S26 implement them with real database data.
